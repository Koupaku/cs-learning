# useEffect「五练」训练计划 🔁🌤️

> 学员反馈（2026-09-11）：**一节课时间太短，新函数想花 3~5 次训练完全理解。**
> 所以从现在起，每个新函数/新概念都走「**多练制**」：拆成 3~5 次短训练，每次 25~40 分钟，一次只做一练。
> 本次对象：**useEffect**（第 5 课开始改用训练制，讲义 `notes/16` 变成"参考手册"，不再是"一节课上完"）

---

## 0. 为什么改成"多练"（给自己看）

一次课里塞进"看懂 + 记住 + 会用 + 上手项目"，大脑要同时干四件事，结果每件都只到 60%。
拆成五练，每一练只要求"这一件事达标"，**每次都成功 → 自信攒起来 → 反而更快**。

对应关系：

| 原来（一节课） | 现在（五练） |
|----------------|--------------|
| 讲原理 + 看打样 + 抄代码 + 写练习 + 进项目（一口气） | 看懂 → 默写 → 变式 → 上身 → 讲出来（分五次，每次一个目标） |

---

## 1. 训练周期总表（**以后每个新函数都照这个走**）

| 练 | 名字 | 这一练只干这件事 | 产出物 | 时长 |
|----|------|------------------|--------|------|
| **训练 1** | 🧐 看懂 | 跑起打样 → 观察现象 → 手敲一遍 | 打样副本（自己敲的） | 30~40 分 |
| **训练 2** | ✍️ 默写 | 闭卷默写最小可用版（不查资料） | 一个能跑的小组件 | 20~30 分 |
| **训练 3** | 🔀 变式 | 换个场景/换个参数，用同一个函数再写一遍 | 第二个小程序 | 25~40 分 |
| **训练 4** | 🚀 上身 | 用进 `react-weather` 真实功能里 | 项目里的一处真功能 | 40~60 分 |
| **训练 5** | 🗣️ 讲出来 | 用自己的话写词汇手册 + 回答面试题 + 讲给我听 | 手册条目 + 讲解 | 20~30 分 |

**六条规矩（写在墙上）**

1. 一次只做一练，**做完就提交 Git**（提交信息写成 `训练1：useEffect打样` 这样）
2. 卡住超过 15 分钟就问我，把报错原文贴过来
3. 一练没通过**就重做同一练**——允许第 6 次、第 7 次，宁慢不错
4. 新函数最多同时推进 **1 个**（不要 useEffect 没吃透就开 useRef）
5. 每一练都要**能跑起来看到结果**，不许"只看不写"
6. 每练结束我问一句"这练你最大的收获是什么"——答不上来说明没消化，再来一遍

---

## 2. useEffect 的专属五练表（我们的路线）

| 练 | 目标 | 具体任务 | 验收标准 | 状态 |
|----|------|----------|----------|------|
| 1 | 看懂"什么时候跑" | 跑 `src/demo/UseEffectDemo.jsx`（4 个零件），回答问题，手敲副本 | 能说出"不写数组 / `[]` / `[x]`"三者的区别 | ✅ **通过**（2026-09-11，零件 1~4 全敲完） |
| 2 | 默写 + 清理函数 | 闭卷写 `Clock.jsx`：`useEffect` + `setInterval` + `return () => clearInterval` | 不查资料能写出来；开关/卸载不"漏"定时器 | ⬜ |
| 3 | 变式：换场景 | `CityClock.jsx`：显示当前时间 + 一个"依赖数组盯着城市"的小件（`[city]` 一变就重算） | 能自己决定依赖数组里该写谁 | ⬜ |
| 4 | 上身：进项目 | `src/components/WeatherCard.jsx`：fetch → state → loading/error → 城市按钮 + `[city]`（讲义 `notes/16` 第 3~5 步） | 讲义 §5 的四条验收全过 | ⬜ |
| 5 | 讲出来 | 往 `notes/前端词汇手册.md` 加 `useEffect` / 依赖数组 / 清理函数；回答 `notes/16` §6 的四个面试题 | 能不用看笔记讲 3 分钟 | ⬜ |

---

## 3. 训练 1 讲义（本次，30~40 分钟）

### 一句话人话解释

> `useEffect` = 给组件装个"闹钟"，对 React 说：**"重画完了之后，顺便帮我做这件事。"**
> 第二个参数（依赖数组）= 你**盯着谁**——盯着的人变了，闹钟才重新响。

### 为什么需要它（30 秒版）

组件函数会被 React **反复调用**（每次 state 变就重跑一遍）。所以联网、定时器这种"渲染之外的动作"不能直接写在函数体里——不然你每敲一个字就发一次网络请求。`useEffect` 就是用来**控制这些动作什么时候执行**的地方。

### 你要做的事（照着走，每步都能看到结果）

1. **启动**（终端里，在 `react-weather/` 下，你的 App.jsx 我已经改好了）：
   ```bash
   npm run dev -- --host 127.0.0.1
   ```
   打开 http://127.0.0.1:5173/ ，按 **F12** 切到 **Console** 面板。
2. 页面会出现四个盒子。**挨个点按钮，盯着 Console**，别急着看代码。
3. 三个观察任务（答案写在回复里给我，或写进代码注释）：
   - **观察 A**：零件 1 和零件 2 我都点了 5 次 `+1`。它俩的打印有什么不一样？
   - **观察 B**：零件 3 里我一边点按钮一边在输入框打字。打字时 Console 为什么**不**多一条？
   - **观察 C**：零件 4 反复按"开始/暂停"，Console 是怎么成对出现的？"拆掉定时器"这条如果**永远不出现**，会发生什么？
4. **手敲一遍**：新建 `src/demo/UseEffectDemoCopy.jsx`，**照着打样手敲**（不要复制粘贴），只敲**零件 1~3**（零件 4 下一练再敲）。
   敲完把 `App.jsx` 里的 `UseEffectDemo` 改成 `UseEffectDemoCopy`，确认页面一样能跑。
   > 抄写是好事（你有这个习惯），但**敲到哪一行就在心里默念它在干嘛**——比如敲到 `}, [count])` 时问自己："我盯的是谁？"

### 验收（全过 → 训练 1 通过）

- [ ] 能说出三种写法的区别：不写数组 / `[]` / `[count]`
- [ ] 三个观察任务都有自己的答案（哪怕猜的）
- [ ] `UseEffectDemoCopy.jsx` 手敲完成且能跑
- [ ] 提交：`git add .` → `git status`（不能有 node_modules）→ `git commit -m "训练1：useEffect打样与手敲"` → `git push`

---

## 4. 训练 2~5 概要（下次课我再展开成详细讲义，先不用管）

- **训练 2 · 默写**：我给你一个空文件 `src/components/Clock.jsx`（只有 TODO 注释），你闭卷写"每秒走一格的时钟"。重点是**清理函数**——不清理的话，React 会攒一堆定时器。
- **训练 3 · 变式**：从 `setSeconds` 换成 `new Date()` 显示"当前时间"；再练"依赖数组该写谁"（练习：`[city]` 和 `[]` 分别会发生什么）。
- **训练 4 · 上身**：把 `notes/16` 的第 3~5 步搬进真实的 `WeatherCard.jsx`（fetch + loading/error + 城市按钮）。这一练做完，第五课才算真正完成，App.jsx 换回 TodoApp 或天气卡片。
- **训练 5 · 讲出来**：写词汇手册 + 面试四问 + 给我讲一遍。

---

## 5. 上次课遗留的两个问题（我答，请对照看）

### Q1（`react-weather/src/components/TodoApp.jsx` 第 18 行注释）

> "这里 addTodo 传进来的数是 input？怎么和 input 联系上的？"

**答：`addTodo` 其实一个参数都没有**，它是"空手"函数：

```jsx
function addTodo() { ... }          // ← 括号里是空的，没人"传"东西给它
<button onClick={addTodo}>添加</button>   // ← 传的是【函数本身】，不是它的结果
```

那它凭什么能用到 `input`？因为 **`input` 是同一个组件里的 state 变量**，`addTodo` 定义在组件函数内部 →
**站在里面就能直接看见外面那层冰箱上的便利贴**，不需要别人递进来。

> 一句话记忆：**在组件里，state 像贴在冰箱上的便利贴——组件内任何函数抬头都能看见它，不用传参。**

对比一下你另一处写法，就明白"传参"和"读外层"的区别：

| 写法 | 谁给值 | 什么时候 |
|------|--------|----------|
| `addTodo` 里读 `input` | 不用给，自己抬头看 state | 读外层变量（闭包） |
| `toggleDone(todo.id)` | 调用时**当场递进去**一个 id | 传参 |

顺带解释你可能会问的：`onClick={addTodo}` 不加括号 = **把函数本身交出去**；React 点击时会替我们调用它，
还会塞一个"事件对象"进去（你没写参数，所以那个对象被忽略掉了，没关系）。

### Q2（`notes/08-展开与不可变性打样/index.js` 第 49 行的问句）

> "`id === 1` 的意思是 `id === true` 吗?"

**不是。** `===` 是"**相等比较**"，结果才是 `true` 或 `false`：

```js
1 === 1        // → true   （读作："1 等于 1 吗？"）
t.id === 1     // → true/false（"这条的 id 等于 1 吗？"）
```

`1` 只是一个**数字**，不是什么"真值"；它跟 `true` 没关系。

顺便记住两条易错点：
- `=` 是**赋值**（把右边塞给左边），`===` 才是**比较**——练习 04 的 `if (running = true)` 就是栽在这。
- 比较必须是"两个东西比"，`t.id === 1` 左边是变量、右边是数字，比的是**值**。

### 附：你那两条理解注释（我核对过）

- 第 23 行"筛掉 id 正确的（目标），留下……" → ✅ 对。更精确：`filter` 的条件是 `todo.id !== id`，
  **留下"id 不等于被点那个"的**，等于"把被点的那条踢出去"。
- 第 29 行"遍历 todos 找到相同 id 的 `{}`，把 done 反转，否则不变" → ✅ 完全正确，就是这个意思。

---

## 6. 词汇手册待加（训练 5 时用你自己的话写进去）

| 名字 | 一句话（先别看，自己写） |
|------|--------------------------|
| `useEffect(函数, 依赖数组)` | 重画之后再做的那件事；依赖数组 = 我盯着谁 |
| 依赖数组 `[]` | 谁也不盯 → 只在首次挂载后跑一次 |
| 副作用（side effect） | 渲染之外的动作：联网、定时器、localStorage |
| 清理函数 `return () => {}` | 走之前把东西收拾好（关定时器、取消订阅） |
| StrictMode 双跑 | 开发模式故意跑两次，帮你发现没清理干净的副作用 |
| **闭包旧值** | effect/定时器里的变量永远是"出生那一轮"的值（你在零件 2 自己撞见过） |
| **函数式更新** | `setX(prev => ...)`，拿最新值算新值 |

---

## 7. 训练 1 检查结果（2026-09-11）✅

**结论：通过。** 你超额完成了任务——讲义只要求手敲零件 1~3，你把**零件 4（定时器 + 清理函数）也敲完了**，
而且零件 4 的注释把执行流程写得**完全正确**（`setRunning` → 反转 → 重画 → 执行 effect → `running === true` 跳过 return → 装定时器）。
这份"能自己追踪执行顺序"的能力，就是训练 1 真正要拿到的东西。👏

### 逐行点评

| 位置 | 点评 |
|------|------|
| 零件 4 注释（第 108~110 行） | ✅ **全对**。你已经能"顺着代码把时间线走一遍"，这比背结论强得多 |
| 零件 4 的 `return () => { clearInterval(timer) }` | ✅ 清理函数写对了，位置也对（在 effect 函数最后） |
| 零件 3 的 `onChange={(e) => setText(e.target.value)}` | ✅ 照抄准确，`e.target.value` 一个字母没错（你有拼写前科 😄，这次很干净） |
| 三个零件都自己动手敲了 | ✅ 这就是训练 1 的意义 |
| 顶部的"教法建议"（第 3~12 行） | ✅ **采纳**——写得非常到位，已经变成 `notes/18-React写法读法卡.md`，并写进交接文档的教学法 |

### 要改的 3 处（小修，1 分钟）

| # | 位置 | 现在 | 改成 | 为什么 |
|---|------|------|------|--------|
| 1 | `UseEffectDemoCopy.jsx` 第 55 行 | `const [text, setText] = useState(0)` | `useState('')` | `text` 存的是**文字**，初始值该是**空字符串**。用 `0` 输入框一开始显示 "0"，而且"数字 state 配文字输入框"类型不一致 |
| 2 | 第 79、101 行 | `setRuning` | `setRunning` | 拼写少了 `n`。**这次能跑是因为前后错得一致**；只改一处就会 `is not defined` |
| 3 | 零件 2 第 37~38 行 | 打印 `count` 但依赖是 `[]` | **保留，别改** | 你无意中做出了"**闭包旧值**"的活演示：点几次 `+1`，打印出来的 `count` **永远是 0**。留着当教材（见 §8-Q5 和读法卡 §5） |

> 另外：`App.jsx` 现在导的还是**我的** `UseEffectDemo`，不是你的 Copy。想跑自己的版本，把第 5 行改成
> `import UseEffectDemo from './demo/UseEffectDemoCopy.jsx'`（组件名一样，只改路径）。

---

## 8. 训练 1 的五个疑问（逐条回答 · 详解见 `notes/18-React写法读法卡.md`）

### Q1「useEffect 箭头指向的、由 `{}` 包裹的函数，这个叫什么？」
→ 叫 **回调函数（callback function）**——"交给别人、等别人在合适时机替我调用"的函数。
交给 `useEffect` 的这个，专业叫 **副作用函数 / effect 回调**；交给 `setInterval` 的叫定时器回调；交给 `onClick` 的叫事件处理函数。
**共同点：你写好了不自己调，交给 API 去调**——这就是"回调"的含义。→ 读法卡 §3

### Q2「{A, B} 里 A 和 B 分别代表什么，要怎么引用？」
→ 那是我写的**签名占位**：`A` = 第一个位置 = **函数**，`B` = 第二个位置 = **数组**。
它们**不是变量名**：代码里直接写 `useEffect(() => {...}, [count])`，不需要给 A、B 起名，也没法在别处引用它们。
（类比 `addEventListener('click', fn)`：第一个位置填"事件名"，第二个位置填"函数"。）→ 读法卡 §2

### Q3「onChange 的读法？第一次出现的方法或函数都应该解释它」
→ **承诺生效**：以后新写法一律先给**读法卡**（括号填什么 / 箭头是什么 / 返回什么 / 怎么接）。
`onChange={(e) => setText(e.target.value)}` 逐段读法：
"**当这个输入框内容改变时**（onChange），**收到一个事件对象 e**（React 塞进来的），**把触发事件的元素（`e.target`，就是输入框自己）的当前内容（`.value`）写进 text 状态**"。→ 读法卡 §8

### Q4「为什么"拆掉 timer"没被执行？一个函数里有 2 个 `return`，和 `if` 是什么关系？」
→ **关键澄清：那两个 `return` 不属于"同一个函数里的两条出口"。**
- `if (!running) return` → 结束的是**这次 effect 执行**（提前收工，后面全不跑）
- `return () => {...}` → 是这次 effect 的**返回值**，返回**一个函数**，**交给 React 保管**，不是立刻执行

**类比**：装定时器时你顺手交给 React 一张"善后说明书"，React 拿着站旁边，**到该拆的时候才照做**。
"该拆"只有两种时刻：① `running` 变了（重装前先拆旧的）② 组件被移除。
所以现在看不到"拆掉 timer"，只是因为**还不到时候**——点"暂停"它立刻出现。→ 读法卡 §4

### Q5「能写成 `setTodos((prev) => ({...prev, {...}}))` 吗？」（TodoApp 第 18 行）
→ **不能，但方向完全正确**（想用函数式更新，这个直觉很准）。两个错：
1. `{ ...prev }` —— `prev` 是**数组**，摊进 `{}` 会变成**对象**（`{0:..,1:..}`），`map` 立刻崩
2. `{ ...prev, { id: ... } }` —— 对象里每一项必须是 **`键: 值`**，光秃秃一个 `{}` 是**语法错误**

**✅ 正确写法**：`setTodos((prev) => [...prev, { id: Date.now(), text: input, done: false }])`
**口诀：中括号 `[]` 造数组，大括号 `{}` 造对象——括号形状要跟"我要造什么"一致。**
（你写的圆括号 `({...})` 只在"箭头函数直接返回**对象**"时才需要。）→ 读法卡 §6

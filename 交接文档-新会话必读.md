# 交接文档 · 新会话必读 🤝

> 用途：本文件是「编程学习陪练项目」的完整交接说明。
> 新会话的 AI agent 请**先读完本文件**，再读 `学习路线图.md` 顶部的「进度打卡」，然后继续教学。
> 工作区固定为：`D:\YMZZZZZ\STUDY\ComputerScience`（不会变）
> 最后更新：**2026-09-11**（阶段 3 React · useEffect「五练」**训练 1 已通过 ✅**，下一练 = 训练 2「默写」）

---

## 0. 30 秒速览

| 项目 | 内容 |
|------|------|
| **学员** | 零基础起步（自称"学过一点语法"，但起点连 `map/filter/reduce` 都不会） |
| **终极目标** | 做出**作品集**，在**面试**中拿到好成绩（目标：前端 / 全栈开发） |
| **节奏** | 每天 1~2 小时，一年期规划；**每个新函数拆成 3~5 次训练**（★ 见 §2.1） |
| **当前阶段** | 阶段 3「React」——课 1~4 完成；第五课 useEffect **训练 1 ✅ 通过** → 下一步**训练 2（闭卷默写 `Clock.jsx`）** |
| **仓库** | https://github.com/Koupaku/cs-learning （分支 `main`，GitHub 用户名 Koupaku） |
| **教学法** | **读法卡优先** + 打样教学 + 分步走 + **一函数多练（3~5 次训练）** + 每练必提交 Git（详见 §2） |

---

## 1. 项目目标与路线图

### 终极目标
做出一份**能拿出手的作品集**，在面试中拿到好结果。方向：**前端 / 全栈**。

### 六个阶段（详见 `学习路线图.md`）
| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | 环境搭建（VS Code / Node / Git / GitHub） | ✅ 完成 |
| 1 | JavaScript 深入（数组方法、DOM、事件、闭包、定时器、异步） | ✅ **毕业**（练习 01~05） |
| 2 | HTML/CSS（盒模型、Flexbox、Grid、响应式、动画） | ✅ **毕业**（练习 06~07） |
| 巩固期 | 不可变性 + ES 模块（React 前置） | ✅ **完成**（练习 08~09） |
| 3 | **React**（组件/props/state/hooks/路由/项目/部署） | 🔄 **进行中**（课 1~4 完成；useEffect 五练 · 训练 1 ✅ / 训练 2 待做） |
| 4 | 后端入门（Node/Express、数据库、鉴权、部署） | ⬜ 未开始 |
| 5 | 作品集收尾（3 个项目打磨 + 个人网站 + GitHub 门面） | ⬜ 未开始 |
| 6 | 面试准备（算法基础、前端八股、项目深挖、模拟面试） | ⬜ 未开始 |

---

## 2. ⭐ 教学法与学员画像（新 agent 必须遵守）

### 学员已明确要求的学习方式（原话："以后出现新的函数，也像这样给我教学与打样，不然实在是有点看不懂"）

每个新函数/新概念 → **① 一句话人话解释 → ② 在 `notes/` 放一个可运行的"打样"文件（逐行中文注释）→ ③ 学员先跑通看效果 → ④ 再做练习**。

打样的参考风格：学员朋友写的对象教程（`notes/sample/index.js`）——**可运行 + 每行中文注释 + 循序渐进**。

### 2.1 ★ 训练制（学员 2026-09-11 要求，最高优先级）

> 学员原话："我觉得一节课持续的时间太短了，对于新的函数，我希望花 3~5 次训练来完全理解它。"

**从此每个新函数/新概念都走「多练制」，不再"一课塞完"。** 通用模板（每次 25~40 分钟，一次只做一练）：

| 练 | 名字 | 这一练只干这件事 | 产出物 |
|----|------|------------------|--------|
| **训练 1** | 🧐 看懂 | 跑打样 → 观察现象 → 手敲一遍 | 自己敲的打样副本 |
| **训练 2** | ✍️ 默写 | 闭卷默写最小可用版（不查资料） | 一个能跑的小组件 |
| **训练 3** | 🔀 变式 | 换场景/换参数，用同一个函数再写一遍 | 第二个小程序 |
| **训练 4** | 🚀 上身 | 用进 `react-weather` 真实功能 | 项目里的一处真功能 |
| **训练 5** | 🗣️ 讲出来 | 自己的话写词汇手册 + 面试问答 + 讲给教练听 | 手册条目 + 3 分钟讲解 |

**六条规矩（教学时不得违反）**
1. 一次只做一练，**做完就提交 Git**（提交信息形如 `训练1：useEffect打样与手敲`）
2. 学员卡住 15 分钟就问，教练要**要报错原文**
3. 一练没通过**就重做同一练**——允许第 6、第 7 次，宁慢不错
4. **最多同时推进 1 个新函数**（useEffect 没吃透不开 useRef）
5. 每一练都要**能跑起来看到结果**，不许"只看不写"
6. 每练收尾必问一句"**这一练你最大的收获是什么**"；答不上来 → 说明没消化，再来一遍

**当前函数的五练表**：`notes/17-useEffect训练计划.md`（每完成一练，回填状态列 + 追加"检查结果/疑问解答"章节）。
**未来的新函数**：照样先建一个 `notes/NN-XXX训练计划.md`，把五练写清楚再开练。

### 2.2 ★ 读法卡优先（学员 2026-09-11 第二个要求，同样必须遵守）

> 学员原话（写在他手敲文件的顶部注释里）：
> ① "对于 useEffect，我更希望**优先读到它的写法所代表的含义**，特别是出现了**括号或者箭头**的情况。"
> ② "应该这样介绍 `useState`：`useState()` 括号需要填写**初始值 initialState**，**返回一个数组 [当前状态, 更新状态的函数]**，可以用 JS 的**解构写法**……"
> ③ "并且在 **React 特殊语法的地方进行区分**。"
> ④ "第一次出现的**方法或者函数就应该解释它**。"

**落地方式**：讲任何新 API 时，**先给"读法卡"，再讲现象/例子**。读法卡七格（模板见 `notes/18-React写法读法卡.md` §0）：

| 格 | 问题 |
|----|------|
| ① 写法 | 原样抄一遍 |
| ② 括号里填什么 | 几个位置？哪个必填、哪个可选？ |
| ③ 返回什么 | 数组？函数？undefined？ |
| ④ 怎么接住 | 解构 / 起什么名 / 能不能忽略 |
| ⑤ 一句中文读法 | 把符号读成人话 |
| ⑥ ★ 哪部分是 React 特有，哪部分是普通 JS | 分开标（学员特别要求） |
| ⑦ 易错点 + 会看到的现象 | 提前打预防针 |

**已有的读法卡**：`notes/18-React写法读法卡.md` —— `useState` / `useEffect` / 箭头函数与回调 / 清理函数 / `setX(prev=>)` / `setTodos` 括号形状 / `onClick` / `onChange`+`e.target.value` / `{}` 的三种身份 / 名词速查。
**以后每出现一个新写法**（`useRef`、`props.children`、`useContext`、`<Link>`、`useNavigate`…）→ **先补一张卡进 `notes/18`**，再开始训练 1。

### 教学纪律（踩过坑总结出来的）

| 要做 | 不要做 |
|------|--------|
| 先讲"**为什么需要它**"，再讲怎么用 | 一上来就甩语法 |
| **先给读法卡（括号/箭头/返回值/怎么接），再讲现象** ★ | 只讲现象，让学员自己猜符号含义 |
| 用**生活比喻**（快递箱=盒模型、字典=对象、擂台赛=reduce、奶茶店小票=Promise、施工队长 vs 设计师=命令式 vs 声明式、透明保鲜膜=`<>`、闹钟=useEffect、**善后说明书=清理函数**） | 用抽象术语堆砌 |
| **第一次出现的方法/函数/属性，当场解释读法** ★ | 默认他已经懂了 |
| 给**小步可验证**的任务（每步都能跑、都能看到结果） | 一次给一大块代码让他自己组装 |
| 给代码时要求他**亲手敲**并运行验证 | 只给链接/只讲不练 |
| 出错时先**安抚情绪**，再定位问题 | 直接指出"你错了" |
| **一个函数分成 3~5 次训练**（见 §2.1） | 一节课塞 5 个新 API / 硬把一课上完 |
| **观察任务要"看得到差异"**（别把答案直接抄在打样提示里） | 让观察题自带答案（训练 1 犯过这个错） |

### 学员画像（重要）

- **中文交流**，全程用中文教学
- 学习速度快（短期内完成 9 个练习 + React 前 4 课），但**自信心脆弱**：会说"我完全不会""我是哪里有错吗""这让我感觉很受挫"——**这类信号出现时，先肯定进步、再拆小步骤，不要继续加难度**
- **明确提过"一节课太短"** → 他需要的是**更多的重复**，不是更多的新知识（见 §2.1）
- **会反过来给教法提要求**（2026-09-11 写了一段"应该这样介绍 useState…"）——**他的要求优先级高于我原来的讲法**，要当场采纳并写进文档
- 会把**疑问写在代码注释里**请你检查（会明确说"看注释里的备注"）——**务必逐条回答他的注释问题**，这是他的主要提问渠道
- 有**"抄写/默写"练习习惯**（会重抄代码、会留 `xxxcopy.js` 文件）——这是好事，鼓励；训练 1 就靠它
- **拼写事故高发**（`fliter`→`filter`、`respone`→`response`、`texxContext`/`textContext`→`textContent`、`avatat`→`avatar`、`setRuning`→`setRunning`）——每次检查都要专门扫一遍拼写
- 会提出**很有深度的细节问题**（"addTodo 怎么和 input 联系上的？"、"为什么拆掉 timer 没被执行？两个 return 和 if 什么关系？"、"能写成 `setTodos(prev => ({...prev, {...}}))` 吗？"）——要认真回答，这是他理解力的体现
- 曾经在 JSX 阶段卡死过一次，用"**两区模型 + 六步分步建**"（`notes/15`）救回来了——**"分步走 + 多练"对他极其有效**

### 协商好的协作约定（写在 `学习路线图.md` 里）
1. **作业制**：每次布置 1 个练习/一练，做完推 GitHub
2. **代码审查**：他发结果 → 逐行点评 + 给改进建议
3. **共做项目**：大项目拆任务清单
4. **随时答疑**：卡住 15 分钟就问，贴报错原文
5. **打样教学**：每个新概念配可运行示例
6. **★ 一函数多练**：新函数拆成 3~5 次训练（见 §2.1）
7. **★ 读法卡优先**：新写法先给"读法卡"（见 §2.2）

---

## 3. 环境与工作区事实

### 工具版本（已装好，无需重装）
| 工具 | 版本/状态 |
|------|-----------|
| Node.js | v24.19.0 |
| npm | 11.17.0 |
| Git | 2.55.0.windows.3（已配 Git Credential Manager，推送时弹浏览器登录） |
| Python | 3.14.6（暂未用于本项目） |
| VS Code | 1.131.0，扩展：中文语言包、Python 系列、Remote-WSL、**Live Server**、pdf |
| 浏览器 | 用于运行网页练习 |

### ⚠️ 重要：学员**主动关闭了 VS Code 的智能提示**（为了"闭卷练习"）
`settings.json` 里关掉了：`quickSuggestions`、`suggestOnTriggerCharacters`、`wordBasedSuggestions`、`inlineSuggest`、`parameterHints`、`acceptSuggestionOnEnter`。
**所以他没有自动补全**——不要以为编辑器坏了；也不要建议他打开（他说过"我不想做开卷考试"）。可以提醒他**看编辑器里的黄色波浪线**（诊断还在，能抓拼写错）。

### Git 仓库
- **仓库根 = `D:\YMZZZZZ\STUDY\ComputerScience`**（练习 01 时 `git init` 的）
- **`react-weather/` 没有独立 `.git`**，属于同一个仓库（**严禁在里面再 `git init`**）
- remote：`https://github.com/Koupaku/cs-learning.git`，分支 `main`
- 日常流程（每次都这样）：`git add .` → `git status` 检查（**不能出现 node_modules**）→ `git commit -m "..."` → `git push`
- 提交信息风格：中文，如 `练习09 模块化天气卡片`、`react环境 + 第三课 useState`、`训练1：useEffect打样与手敲`

### React 项目（react-weather）
- 位置：`react-weather/`，用 `npm create vite@latest react-weather -- --template react` 创建（**Vite v8.2.2 + React 19，构建器是 rolldown/oxc，没有 esbuild**；lint 用 `npm run lint`（oxlint，能抓 `react-hooks/exhaustive-deps`））
- **启动命令（必须带 host 参数）**：`npm run dev -- --host 127.0.0.1`
- 访问地址：**http://127.0.0.1:5173/**
- ⚠️ **已知问题 1**：不加 `--host 127.0.0.1` 时，Vite 只监听 IPv6 的 `::1`，浏览器用 IPv4 连不上（已解决，命令里带上即可）
- ⚠️ **已知问题 2（未解决，学员已接受）**：**HMR 热更新在浏览器侧不生效**（Vite 终端会打印 `hmr update`，但页面不自动变）→ **改完代码手动 `Ctrl + R` 刷新**。不要在这一点上花太多时间
- 目录约定：组件放 `src/components/`，演示/打样组件放 `src/demo/`
- 现有文件：`components/ProfileCard.jsx`、`components/TodoApp.jsx`（第四课成果）、`demo/JsxRulesDemo.jsx`、`demo/UseEffectDemo.jsx`（**我写的训练 1 打样**）、`demo/UseEffectDemoCopy.jsx`（**学员手敲的副本**）
- `App.jsx` **当前指向 `<UseEffectDemo />`**（我的打样版）。学员想跑自己的副本要改成 `./demo/UseEffectDemoCopy.jsx`（组件名相同，只改路径）。**训练 4 结束后换回 `<WeatherCard />`**

### 工作区文件约定
| 路径 | 用途 | 命名规则 |
|------|------|---------|
| `学习路线图.md` | 总纲 + **顶部「进度打卡」**（每课/每练完成后更新 checkbox） | 固定 |
| `exercises/NN-名字/` | 练习作业（README.md 写需求/提示/验收/提交步骤 + 骨架文件含 TODO 注释） | 两位数递增 |
| `notes/NN-主题/` 或 `notes/NN-标题.md` | 打样文件（可运行+逐行注释）或讲义 | 递增（**注意：编号 14 被跳过**，目前到 18） |
| `notes/NN-XXX训练计划.md` | **新函数的「五练」表**（总表 + 每练讲义 + 检查结果 + 疑问解答） | 递增（现有 `17-useEffect训练计划.md`） |
| `notes/18-React写法读法卡.md` | **读法卡总集**（括号/箭头/返回值/React 特殊性）——新写法往里加卡 | 固定（与 17 同类，编号相邻） |
| `notes/前端词汇手册.md` | 学员的私人词典（持续追加新 API，用他自己的话） | 固定 |
| `notes/自学资源与查文档指南.md` | 查文档五步法 + MDN/视频资源清单 | 固定 |
| `react-weather/` | React 主项目（阶段 3 主战场） | 固定 |
| `sentence-reader/` | 学员自己的产品设计文档（未来大项目，见 §6） | 固定 |

---

## 4. 已完成清单（含关键技能点）

### 阶段 0：环境（✅）
VS Code / Node v24 / Git / Python 装好；GitHub 注册；**第一次 push 成功**。

### 阶段 1：JavaScript 深入（✅ 毕业）
| 练习 | 内容 | 关键技能 | 踩过的坑 |
|------|------|---------|---------|
| 01 成绩分析器 | `exercises/01-score-analyzer/` | map / filter / reduce / 模板字符串 / 展开运算符 / `toFixed` | 浮点误差（`96.60000000000001`）→ `Math.round(x*100)/100`；作用域（`top is not defined`）；参数与实参 |
| 02 猜数字游戏 | `exercises/02-guess-number/` | DOM（getElementById/textContent）、addEventListener、Math.random/floor、if-else 三选一 | **if 缺大括号**导致 gameOver 每次都执行；**变量遮蔽**（`const target` 遮住外层）；`display='yes'` 非法值 |
| 03 待办清单 | `exercises/03-todo-list/` | 数据驱动（数组→render）、createElement/appendChild、forEach、闭包（删除按钮记 id） | 两个 render 打架（补丁式开发）；`console.log(不添加)` 缺引号 |
| 04 秒表 | `exercises/04-stopwatch/` | setInterval / clearInterval、running 标志防重复 | **`if(running = true)` 赋值当比较**（经典坑）；`()=>tick` 只引用不调用；多余的 `start()` 开局调用 |
| 05 天气卡片 | `exercises/05-weather-card/` | **fetch / async / await / try-catch**（第一次联网）、Open-Meteo API、查表取 emoji | JSON 路径少写一层（`data.current_temperature`）；catch 里显示错误而非只 console |

### 阶段 2：HTML/CSS（✅ 毕业）
| 练习 | 内容 | 关键技能 | 踩过的坑 |
|------|------|---------|---------|
| 06 个人名片 | `exercises/06-profile-card/` | 盒模型、Flexbox 居中大法、100vh、border-radius、box-shadow、:hover + transition | **`.body` 写成类选择器**（应为标签选择器 `body`）；`padding: 16px red` 非法；块级元素固定宽度要 `margin: 0 auto` |
| 07 天气卡片 2.0 | `exercises/07-weather-card-v2/` | **Grid（auto-fit + minmax）、响应式、定位（角标）、状态配色**、weathercode→emoji 查表 | `align-self` 用错（应为容器上的 `align-items`）；`minmax(80,1fr)` **漏单位 px**；`.card` 漏圆角阴影 |

### 巩固期（✅ 完成）
| 练习 | 内容 | 关键技能 | 踩过的坑 |
|------|------|---------|---------|
| 08 待办清单 v2 | `exercises/08-todo-v2/` | **展开运算符 / 不可变性**（`[...todos, x]`、`filter` 删、`map + {...t}` 改）、Date.now() 做 id | 无法独立写出 map+展开组合（→ 引出"抄懂→默写→变式"三步法 = 现在训练制的前身） |
| 09 天气卡片模块化 | `exercises/09-weather-modules/` | **ES 模块 import/export**、职责拆分（data→ui→api→main）、Live Server | **跨文件用变量没 import**（`status is not defined`） |

### 阶段 3：React（🔄 进行中）
| 课/练 | 内容 | 关键技能 | 备注 |
|----|------|---------|------|
| 环境 | `react-weather/`（Vite + React） | npm create vite / npm install / npm run dev | IPv6 端口坑（见 §3） |
| 课 1 | `notes/10-React第一课-组件与JSX.md` | **声明式 vs 命令式**、JSX 三规则（单根/className/`{}`）、组件=函数 | 清理模板自带 `index.css` |
| 课 2 | `notes/11-React第二课-props与组件拆分.md` | **props**（解构接收、单向、只读）、组件独立文件 + import | — |
| 课 3 | `notes/12-React第三课-state与事件.md` | **useState**、`onClick={() => setX(...)}`、重画机制 | 复习"引用 vs 调用"与不可变性 |
| 课 4 | `notes/13-React第四课-待办清单.md` | **受控输入**（value+onChange）、数组 state 三招式、列表 + key、条件 class | ✅ 已独立完成 `TodoApp.jsx` |
| 急救课 | `notes/15-JSX思维急救课与分步建应用.md` | **JSX 两区模型**、函数清点表、**六步分步建** | 学员曾卡死，用此法救回 |
| 课 5 · 训练 1 | `notes/17-useEffect训练计划.md` §3 + `demo/UseEffectDemo.jsx` | 副作用 / 依赖数组三写法 / 提前返回 / 清理函数 | ✅ **通过**（2026-09-11）：手敲零件 1~4，能自己追踪执行顺序 |
| 课 5 · 训练 2 | 待做 | **闭卷默写** `Clock.jsx`（useEffect + setInterval + 清理函数） | ⬅️ **下一步** |
| 读法卡 | `notes/18-React写法读法卡.md` | useState / useEffect / 箭头与回调 / 清理函数 / `setX(prev=>)` / `onClick` / `onChange` / `{}` 三种身份 | ✅ 应学员要求新建（2026-09-11） |
| 演示 | `react-weather/src/demo/JsxRulesDemo.jsx` | JSX 五条规则逐条演示（含故意报错） | 可复用教学道具 |
| 演示 | `react-weather/src/demo/UseEffectDemo.jsx` | useEffect 四零件（不写数组 / `[]` / `[count]` / 定时器+清理） | 训练 1 专用；训练 2~3 还能复用零件 4 |

### 学员已掌握的核心认知（可复用，不要重教）
- 属性（名词，无括号）vs 方法（动词，带括号）；`=` 赋值 vs `===` 比较
- 静默 bug 排查：**数据流三步侦察法**（入口/中途/出口插 `console.log`）
- 死代码纪律：**旧版本交给 Git，不要用注释把历史留在文件里**
- 对象：按名字取不按位置；缺字段不报错（"君子协定"）
- 不可变性：更新 = 造新的，不碰旧的；**中括号造数组 / 大括号造对象**
- props 只读 vs state 可变；组件函数会被 React 反复调用
- JSX 两区模型；**`state` 像冰箱上的便利贴**（组件内函数抬头就能看见，不用传参）
- 回调函数 = 交给别人、等别人在合适时机调用的函数（训练 1 学会）

### 正在建立（训练 2~5 期间反复强化，别当成已掌握）
- `useEffect` 的三种依赖写法、清理函数"善后说明书"模型
- **闭包旧值**（他自己在零件 2 撞见过：`[]` 依赖里打印的 `count` 永远是 0）
- **函数式更新 `setX(prev => ...)`**（TodoApp 想过、零件 4 用过，但还没独立用对）

---

## 5. 当前状态（新会话第一件事）

**仓库状态（2026-09-11 核查）**：HEAD = `c905dec "react环境 +  useState 清单"`，**工作区有未提交改动**（学员还没提交训练 1）：

```
 M react-weather/src/App.css                  ← 追加 .demo-* 样式
 M react-weather/src/App.jsx                  ← 暂时指向 <UseEffectDemo />
 M react-weather/src/components/TodoApp.jsx   ← 学员在注释里提问（第 18 行 setTodos 写法）
 M 学习路线图.md / 交接文档-新会话必读.md      ← 教学法更新（多练制 + 读法卡）
?? notes/17-useEffect训练计划.md               ← 五练表 + 训练1 讲义 + 检查结果 + 疑问解答
?? notes/18-React写法读法卡.md                 ← 读法卡总集（应学员要求）
?? react-weather/src/demo/UseEffectDemo.jsx    ← 训练1 打样（我写）
?? react-weather/src/demo/UseEffectDemoCopy.jsx ← 学员手敲副本（训练1 成果！）
```

**提醒话术**：先把训练 1 收尾（改完 §17 的 3 处小修）再一起提交：
```bash
git add .
git status        # 确认没有 node_modules
git commit -m "训练1：useEffect打样与手敲 + 读法卡"
git push
```

---

## 6. 将要做什么（Next Steps，按顺序）

### 立即（下一练）★ 训练 2「默写」
**useEffect 五练 · 训练 2**（讲义待展开，写进 `notes/17` §9）
1. 教练先产出：`react-weather/src/components/Clock.jsx` **空壳**（只留 TODO 中文注释 + 验收清单），**不给答案**
2. 学员**闭卷**写：一个每秒走一格的时钟 —— `useState` 记秒数 + `useEffect` 装 `setInterval` + **`return () => clearInterval(timer)`**
3. 加分项（可选，不强求）：开始/暂停按钮（复用零件 4 那套 `[running]` 依赖）
4. 教会他自查：改完 `Ctrl+R` 刷新；`npm run lint` 应无 warning（**这是第一次让他跑 lint**）
5. 验收：不查资料能写出来；暂停后秒数不再增加；反复开始/暂停不会出现"越走越快"（= 清理函数生效的证据）
6. 收尾问"这一练最大的收获是什么" → 提交 `训练2：闭卷默写时钟`

### 之后（同一函数的后续练习）
- **训练 3 · 变式**：`CityClock.jsx`（显示 `new Date()` 当前时间）+ 依赖数组实验（`[]` vs `[city]` 现象对比）
- **训练 4 · 上身**：`src/components/WeatherCard.jsx` 完整版（`notes/16` 第 3~5 步：fetch → state → loading/error → 城市按钮 + `[city]`），完成后 `App.jsx` 换回 `<WeatherCard />`
- **训练 5 · 讲出来**：`notes/前端词汇手册.md` 加条目（useEffect/依赖数组/清理函数/闭包旧值/函数式更新）+ 回答 `notes/16` §6 四个面试题 + **自己照读法卡七格给 useEffect 写一张卡** + 讲 3 分钟

### 阶段 3 收尾（同样走五练制 + 读法卡）
1. 补课：**组件拆分与回调 props**（父传函数给子）、**useRef**、React 路由（react-router）
2. **给「逐句阅读器」立项**（设计文档 `sentence-reader/docs/设计文档.md`）：导入 TXT → 自动断句 → 逐句点击推进 + 换背景 + 进度保存；M0 可玩原型 → M1 完整阅读体验（书架/设置/PWA）。**第一个真正属于他自己的作品**
3. 阶段 3 项目：个人仪表盘 / 简易记账（或直接以 sentence-reader 为主体）
4. **部署上线**（Vercel：`npm run build` → 导入 GitHub 仓库 → 可选自定义域名）

### 阶段 4~6（各阶段开始前再细化）
- 阶段 4 后端：Node/Express API、数据库（PostgreSQL 或 MongoDB）、JWT 鉴权、部署
- 阶段 5 作品集：精选 3 个项目打磨（README、截图、在线链接）、个人作品集网站、GitHub 主页整理
- 阶段 6 面试：数组/字符串/哈希/递归 + LeetCode 简单题、前端八股（闭包、事件循环、this、虚拟 DOM、React 生命周期、浏览器渲染）、项目深挖演练、模拟面试

---

## 7. 待清理的技术债（不紧急，找合适时机）

1. **练习稿文件散落仓库**：`exercises/03-todo-list/123.js`、`123.html`、`exercises/04-stopwatch/copied.html`、`copied.js`、`exercises/05-weather-card/appcopy.js`、`exercises/06-profile-card/indexcopied.css`——抄写练习产物。**建议问他要不要保留**（保留=学习痕迹，删除=仓库整洁）。`demo/UseEffectDemoCopy.jsx` 同理
2. **HMR 不生效**（见 §3），学员已接受手动刷新
3. `sentence-reader/` 目录**尚未提交**（等他决定是否作为正式项目）
4. `notes/` 编号跳过 14（历史原因，不影响使用）
5. `notes/17` 五练表要**每练完成后回填**（状态列 + 新的"检查结果/疑问解答"章节），否则下一练没有依据
6. `TodoApp.jsx` 里学员的注释问题**已答**（`notes/17` §5、§8-Q5），但这些注释还留在文件里 → 等他删（**不要替他删**，那是他的提问痕迹）

---

## 8. 新会话恢复步骤（照着做即可）

1. **读本文件**（你正在读）
2. **读 `学习路线图.md` 顶部「进度打卡」**（最新进度 + 日期）
3. **查仓库状态**：`git log --oneline -5` + `git status`（确认有无未提交改动，见 §5）
4. **读当前训练文件**：`notes/17-useEffect训练计划.md`（看五练表哪一练是 ⬅️、§7 检查结果、§8 疑问解答）+ 必要时查 `notes/18-React写法读法卡.md`
5. **第一句话建议**（视进度三选一）：
   > 训练 1 未提交时：*"训练 1 我给你批了 ✅ —— 零件 4 都敲完了，执行顺序也追对了。先把 §7 那 3 处小修改掉，然后我们提交，今天开训练 2『默写』。"*
   > 训练 2 未完成时：*"今天训练 2：我给了一个 `Clock.jsx` 空壳，**闭卷**写——每秒走一格的时钟。忘了就自己想，卡 15 分钟再问我。"*
   > 训练 2 完成后：*"训练 2 通过 ✅。今天训练 3「变式」——把秒数换成当前时间，再做个依赖数组实验。"*
6. 然后按 §2.1 / §2.2 的规矩推进（**一次只一练 · 读法卡先行 · 每练必提交 · 卡住先安抚**）

---

## 9. 一句话给新 agent 的提醒

> 这位学员**学得很快，但需要"被扶着走小步"**——给太多会崩，给太少会卡。
> 他明确要求两件事：**新函数要分 3~5 次训练**；**新写法要先讲"括号/箭头/返回值"的含义，并区分哪些是 React 特有的**。
> 每次新概念：**先给读法卡 → 讲为什么 → 给可运行打样 → 分步让他亲手敲 → 每步验证 → 及时肯定 → 提交 Git**。
> 他会在代码注释里问问题（而且问得很深），**记得逐条回答**——那往往是他真正弄懂的地方。
